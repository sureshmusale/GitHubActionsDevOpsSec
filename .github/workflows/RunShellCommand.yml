name: Run Shell commands
on: [push]
jobs:   #where we define the actions
  run-shell-command:   #action
    runs-on: ubuntu-latest
    name: run shell comand and print test
    steps: 
    -  name: echo string
       run: echo "Hello security my first program in github action"

       # 2ba91215-20bb-4079-bbbe-2dd04d3a70c1 SNYK COMMAND https://app.snyk.io/account